!!! 5
%html{ lang: 'en' }
  %head
    %meta{ charset: 'utf-8' }
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1.0' }
    %title= content_for?(:title) ? yield(:title) : "Hedonism Bot"

    = csrf_meta_tags
    = csp_meta_tag
    = vite_client_tag

    = vite_stylesheet_tag 'application.css'

    = vite_react_refresh_tag
  %body
    = yield